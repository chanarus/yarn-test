import React, { useState } from 'react';
import IncrementButton from '@sprii/common';

export default () => {
  // Declare a new state variable, which we'll call "count"

  return (
    <div>
      <IncrementButton text='HOME/SHIPPING/INDEX You clicked' />
    </div>
  );
};
